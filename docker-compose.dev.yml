version: '3'

volumes:
  postgres:

services:
  postgres:
    image: postgres:18-alpine
    restart: unless-stopped
    volumes:
      - postgres:/var/lib/postgresql
    env_file:
      - .env
    ports:
      - "5432:5432"
